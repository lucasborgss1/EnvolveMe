import * as S from "./styles";

export const Footer: React.FC = () => {
    return (
        <S.StyledFooter>
            <span>EnvolveMe - Todos os direitos Reservados</span>
        </S.StyledFooter>
    );
};
